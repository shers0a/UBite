# HTTPS is mandatory: a PWA will not install without it and push will not work (docs/12).
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy app:8080
	header {
		Strict-Transport-Security "max-age=31536000"
		-Server
	}
}
