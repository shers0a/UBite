{
  "name": "@ubite/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types and pure helpers shared by apps/web and apps/api (docs/06).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^5.0.2"
  }
}
